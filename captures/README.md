# Local DDTi captures

This directory is the standard local location for DDTi capture artifacts.
Raw `.syx` files are ignored by Git because they may contain personal module
configuration.  Every captured dump must have a sibling `.hex` and `.json`
integrity record generated by `ddti dump`.

Never modify a golden dump.  Use a new, explicitly named capture for every
controlled experiment.

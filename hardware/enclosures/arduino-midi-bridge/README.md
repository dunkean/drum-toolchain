# Arduino MIDI bridge enclosure

These STL files preserve the two existing enclosure iterations for the Arduino
Uno MIDI bridge:

- `v0`: initial base and lid;
- `v1_precision`: later precision base and lid.

No editable CAD source was present in the workspace when these artifacts were
organized. Keep the STL files versioned until a reproducible source model
replaces them.
